---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ebs-csi
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: storage
  sources:
  - repoURL: 'https://github.com/pelotech/foundation'
    path: 'gitops/components/aws/ebs-csi/snapshot-controller'
    targetRevision: v5.0.3 # x-release-please-version
  - chart: aws-ebs-csi-driver
    repoURL: https://kubernetes-sigs.github.io/aws-ebs-csi-driver
    targetRevision: 2.63.1
    helm:
      # TODO: figure out correct roles for controller vs node
      releaseName: aws-ebs-csi-driver
      valuesObject:
        controller:
          k8sTagClusterId: CLUSTER_NAME
          serviceAccount:
            # Pod-identity-ready by default: no IRSA annotations. Clusters
            # using IRSA use the aws/ebs-csi-irsa component instead (requires EBS_CSI_ROLE_ARN).
            create: true
            name: ebs-csi-driver
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "100m"
              memory: "128Mi"
        node:
          serviceAccount:
            create: false
            name: "ebs-csi-driver"
          resources:
            requests:
              cpu: "150m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "128Mi"
        storageClasses:
          - name: gp3
            allowVolumeExpansion: true
            parameters:
              type: gp3
#              encrypted: "true" #TODO: Encrypting volumes by default seems desirable, check side effects
            annotations:
              storageclass.kubernetes.io/is-default-class: "true"
        volumeSnapshotClasses:
          - name: ebs-snapshot
            annotations:
              snapshot.storage.kubernetes.io/is-default-class: "true"
            deletionPolicy: Delete
  destination:
    namespace: kube-system
    name: in-cluster
  syncPolicy:
    automated: {}
